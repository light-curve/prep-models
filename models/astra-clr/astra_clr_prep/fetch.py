"""AstraCLR has no code submodule — the ONNX is downloaded directly from HuggingFace."""


def run_fetch(*, update: bool = False) -> None:
    print("AstraCLR has no code submodule. Run 'download' to fetch the ONNX file.")
