"""Chronos-Bolt has no code submodule; weights come from the chronos package."""

from __future__ import annotations


def run_fetch(*, update: bool = False, size: str | None = None) -> None:
    print("Chronos-Bolt has no code submodule. Run 'download' to cache model weights.")
