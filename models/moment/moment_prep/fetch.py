"""MOMENT has no code submodule; weights come from the momentfm package."""

from __future__ import annotations


def run_fetch(*, update: bool = False, size: str | None = None) -> None:
    print("MOMENT has no code submodule. Run 'download' to cache model weights.")
